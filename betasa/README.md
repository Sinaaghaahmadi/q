# بتاسا (Betasa)

پلتفرم بازی و سرگرمی رایگان — وبسایت و PWA با **سکه مجازی** و بدون پول واقعی.

> ⚠️ اصل بنیادی محصول: بتاسا یک پلتفرم **تفریحی رایگان** است. هیچ شرط‌بندی با پول واقعی، واریز، برداشت یا جایزه نقدی در آن وجود ندارد. تمام رقابت با سکه مجازی، امتیاز و جوایز غیرنقدی انجام می‌شود.

## ساختار این فضا

| مسیر | محتوا |
|---|---|
| `research/design-research.md` | تحقیق کامل دیزاین (روندهای بین‌المللی و داخلی، تحلیل ۱۰ بازی، مبانی دیزاین‌سیستم) |
| `designs/concept-01-neon-arcade.html` | کانسپت ۱ — نئون آرکید (بین‌المللی، تیره و نئونی) |
| `designs/concept-02-persian-luxe.html` | کانسپت ۲ — لوکس ایرانی (طلایی/فیروزه‌ای، نقوش هندسی) |
| `designs/concept-03-playful-light.html` | کانسپت ۳ — پاپ بازیگوش (روشن، خانوادگی، موبایل‌محور) |
| `designs/concept-04-minimal-pro.html` | کانسپت ۴ — مینیمال حرفه‌ای (اعتمادساز، فین‌تک‌گونه) |
| `designs/concept-05-cyber-bazaar.html` | کانسپت ۵ — بازار سایبری (نسل Z، پرانرژی) |

هر کانسپت یک فایل HTML مستقل است: کافی است در مرورگر باز شود. همه فارسی و راست‌به‌چپ‌اند، هر دو تم روشن/تاریک را دارند (دکمه تغییر تم در هدر)، کاملاً واکنش‌گرا هستند و ۱۰ بازی اصلی را نمایش می‌دهند.

## ۱۰ بازی انتخاب‌شده

کرش، ماین‌ها، پلینکو، تاس، گردونه شانس، بالا/پایین (HiLo)، کِنو، لیمبو، برج، شیر یا خط — دلایل انتخاب در سند تحقیق آمده است.

## صفحه‌ها

| مسیر | چیست |
|---|---|
| `app/index.html` | لندینگ «سالنِ طلا» — دستگاه سکهٔ تعاملی، کفِ سالن، گاوصندوق سکه، نردبان ژتون |
| `app/app.html` | خود اپ (لابی، بازی‌ها، کیف سکه، رکوردها، ورود و پروفایل) — نصب PWA هم به همین‌جا می‌رود |
| `app/design-system.html` | دیزاین‌سیستم زنده |
| `app/visualization.html` | تصویرسازی محصول |

## اپ PWA (`app/`)

پیاده‌سازی نسخهٔ اول PWA بر پایهٔ کانسپت منتخب **۲ — لوکس ایرانی**: استاتیک و بدون وابستگی (HTML/CSS/JS خالص)، فارسی و RTL، هر دو تم، نصب‌پذیر و آفلاین.

- **اجرا:** یک سرور استاتیک روی پوشهٔ `betasa/app` کافی است، مثلاً: `npx serve betasa/app`.
  روی Vercel، پروژهٔ جداگانهٔ `betasa` با ریشهٔ `betasa/app` همین پوشه را منتشر می‌کند.
- **امکانات:** کیف سکهٔ مجازی (شروع ۱۰٬۰۰۰ سکه + جایزهٔ ورود روزانه، ذخیره در localStorage)، لابی با ۱۰ بازی قابل‌بازی (کرش، ماین‌ها، پلینکو، تاس، گردونه، بالا/پایین، کنو، لیمبو، برج، شیر یا خط)، تغییر تم، بنر نصب PWA، manifest + سرویس‌ورکر با کش آفلاین.
- **انصاف بازی‌ها:** همهٔ بازی‌ها با RNG و بازده نظری ≈۹۷–۹۹٪ (لبهٔ ۱٪ در فرمول‌ها مستند است) — صرفاً برای سرگرمی با سکهٔ مجازی.
- **راستی‌آزمایی:** لابی و هر ۱۰ بازی در هر دو تم با مرورگر خودکار چک شده‌اند: صفر خطای JS، صفر سرریز افقی در عرض موبایل، و راندهای واقعی (شیر یا خط، تاس) با تغییر موجودی.

## حساب کاربری

ورود با شمارهٔ موبایل یا حساب گوگل، پروفایل با یوزرنیم، و کد دعوت شش‌نویسه‌ای با هم‌رسانی.
مسیرها: `#/signin` و `#/profile`.

اپ استاتیک است و سروری ندارد، پس آنچه ذاتاً به سرور نیاز دارد «انجام‌شده» وانمود نمی‌شود:

- **پیامک** فرستاده نمی‌شود؛ کد آزمایشی روی صفحه نشان داده می‌شود و خودش می‌گوید که پیامکی در کار نیست.
- **ورود با گوگل** غیرفعال است تا شناسهٔ کلاینت و سرورِ تأیید توکن وصل شود (تأیید توکن هرگز نباید سمت مرورگر باشد).
- **یکتایی یوزرنیم** فقط شکل را می‌سنجد؛ یکتایی واقعی کار سرور است.
- **سهم سکهٔ دعوت‌کننده** در صف `pendingCredits` می‌نشیند، چون هیچ مرورگری نمی‌تواند به کیف دستگاه دیگری سکه اضافه کند.

هر سه درگاه پشت واسط `providers` در `js/account.js` هستند؛ ارائه‌دهندهٔ واقعی همان توابع را پیاده می‌کند و بقیهٔ اپ دست نمی‌خورد.

**هیچ کیف پول ریالی، هیچ واریز و هیچ برداشتی وجود ندارد** — و این یک قابلیتِ نساخته نیست، پایهٔ محصول است.

## گام بعدی

پس از انتخاب کانسپت برنده (یا ترکیب چند کانسپت)، دیزاین‌سیستم نهایی (توکن‌ها، کامپوننت‌ها) استخراج و پیاده‌سازی PWA آغاز می‌شود. صفحهٔ «تصویرسازی محصول» (طبق پرامپت ضمیمه) پس از ساخت نسخهٔ قابل‌اجرا از روی build واقعی تهیه خواهد شد.
