# تاریخچهٔ نسخه‌ها

قالب: هر نسخه یک بخش `## [x.y.z] — تاریخ` دارد. workflow انتشار، بخشِ همان نسخه را
از همین فایل برمی‌دارد و به‌عنوان ریلیزنوت روی GitHub Release می‌گذارد — این‌جا
تنها منبع ریلیزنوت است.

## [1.1.0] — ۱۴۰۵/۰۶/۰۹

آسامیت از «دمو» به **محصول واقعی** تبدیل شد. داده‌های نمونه به‌کل حذف شدند و
حساب‌ها، پیام‌ها و همهٔ داده‌ها حالا واقعی و ماندگارند.

- **ثبت‌نام واقعی با نام کاربری و رمز عبور.** مودال ورود دو تب دارد
  (ورود / ثبت‌نام)؛ رمزها با bcrypt هش می‌شوند، نشست‌ها توکن‌های تصادفی‌اند که
  فقط در کوکی امن `httpOnly` نگه‌داری می‌شوند، و تلاش‌های ناموفق ورود
  محدودسازی می‌شوند. **اولین حسابی که ساخته شود، مدیر بستر است.**
- **پایگاه‌دادهٔ واقعی (Postgres).** کل لایهٔ داده به توابع امن داخل خود
  دیتابیس منتقل شد (`supabase/migrations/`) — پیام‌ها، چت‌ها، گروه‌ها، تماس‌ها،
  جلسات و کلاس‌ها بین دستگاه‌ها مشترک و برای همیشه ماندگارند؛ دیگر هیچ
  داده‌ای با ری‌استارت سرور پاک نمی‌شود.
- **مجوزدهی سمت سرور برای همهٔ مسیرها.** تا قبل از این، سرور به شناسه‌ای که
  کلاینت می‌فرستاد اعتماد می‌کرد؛ حالا هویت هر درخواست از کوکی نشست استخراج
  می‌شود: فقط عضو چت پیام‌هایش را می‌بیند، فقط میزبان جلسه ضبط/پایان را
  کنترل می‌کند، فقط معلم حضورغیاب می‌زند و فقط مدیر به پنل مدیریت و
  دستیار هوش مصنوعی از حساب‌های واردشده در دسترس است.
- **حذف کامل داده‌های نمونه.** ۱۷ کاربر ساختگی، چت‌ها و پیام‌های ساختگی،
  پاسخ خودکار جعلی در چت خصوصی، دکمه‌های «ورود سریع دمو» و رمز مشترک
  «123456» همگی حذف شدند. دموی استاتیک GitHub Pages هم بازنشسته شد.
- **پیام‌رسان زنده‌تر:** شروع چت خصوصی از دکمهٔ «چت جدید» (قبلاً فقط گروه
  می‌شد ساخت)، شمارندهٔ نخوانده و رسید خواندن واقعی per-user، به‌روزرسانی
  خودکار فهرست چت‌ها و پیام‌ها با polling، و وضعیت آنلاین/آفلاین واقعی.
- **پنل مدیریت با آمار واقعی:** نمودار فعالیت هفتگی از داده‌های واقعی محاسبه
  می‌شود؛ تعلیق حساب، نشست‌های کاربر را همان لحظه قطع می‌کند.
- **PWA استانداردتر:** اسکرین‌شات‌های نصب، `display_override`، بازگشت
  pinch-zoom (دسترس‌پذیری)، و Service Worker محتاط‌تر (پاسخ‌های خطا هرگز
  cache نمی‌شوند).
- اپ‌های اندروید به production وصل‌اند و ورود/ثبت‌نام واقعی همان‌جا کار می‌کند.

## [1.0.2] — ۱۴۰۵/۰۶/۰۷

بهبود فرایند انتشار — بدون تغییر در رفتار محصول.

- **نسخهٔ اپ‌های اندروید حالا از تگ انتشار مهر می‌شود.** تا قبل از این، هر APK از
  درون خودش را «1.0» معرفی می‌کرد (پیش‌فرض Capacitor)؛ حالا `versionName` و
  `versionCode` هنگام build از خود تگ ساخته می‌شوند، پس تنظیمات اندروید همان
  نسخه‌ای را نشان می‌دهد که کاربر دانلود کرده.
- **ریلیزنوت از CHANGELOG خوانده می‌شود.** متن هر Release دیگر قالب ثابت نیست؛
  بخش همان نسخه از این فایل برداشته می‌شود، پس ریلیزنوت داخل گیت نسخه‌بندی
  می‌شود و همیشه با کد هم‌قدم است.
- **هم‌ترازی نسخه در هر سه package.json** (وب و هر دو اپ اندروید) که روی 1.0.0
  جا مانده بودند.

## [1.0.1] — ۱۴۰۵/۰۶/۰۶

اولین نسخهٔ امضاشده.

- هر دو APK با کلید انتشار پروژه امضا می‌شوند (زیرساخت v1+v2+v3) و آمادهٔ
  بارگذاری در فروشگاه‌ها هستند؛ `apksigner verify` دروازهٔ انتشار است و اگر
  امضا معتبر نباشد build می‌ایستد.
- APKهای انتشار به production (asameet.online / asameet.vercel.app) وصل‌اند،
  نه به دموی استاتیک.
- خواندن Secretهای امضا در برابر خطاهای paste مقاوم شد: حذف whitespace،
  گزارشِ «شکل» مقدار نامعتبر بدون افشای آن، و تلاش دوباره با رمز store وقتی
  رمز کلید رد شود.
- انتقال به مخزن مستقل `asameet`؛ همهٔ مسیرها و لینک‌ها به خانهٔ جدید اشاره
  می‌کنند و تداخل build با پروژهٔ Asaex برطرف شد.
- سلامت‌سنجِ `GET /api/ai` برای بررسی رایگان وضعیت کلید هوش مصنوعی، با
  تشخیص نام‌های مشابهِ اشتباه.

## [1.0.0] — ۱۴۰۵/۰۶/۰۵

نخستین انتشار عمومی آسامیت — بستر هوشمند گفت‌وگو.

- سایت معرفی (فارسی + ۴ زبان، راست‌به‌چپ، تم روشن/تاریک/سیستم) با صفحات
  مستقل ویژگی‌ها، تعرفه‌ها، درباره ما، سوالات، تماس و سیستم طراحی.
- محصول PWA: پیام‌رسان، تماس، جلسات با ضبط، کلاس آنلاین با تخته، و دستیار
  هوش مصنوعی صورت‌جلسه/خلاصه/هم‌فکری (با حالت دموی بدون کلید).
- پنل مدیریت برای کنترل و نظارت.
- دو اپ اندروید (کامل و فقط پیام‌رسان) از طریق Capacitor، ساخته‌شده در CI.
- زیرساخت استقرار: Vercel، دموی استاتیک GitHub Pages، و Docker/nginx/Prisma
  برای میزبانی روی سرور و دامنهٔ خودی.
