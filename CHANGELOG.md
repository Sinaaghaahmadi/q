# تاریخچهٔ نسخه‌ها — صرافی آسا

قالب: هر نسخه می‌گوید چه چیزی برای استفاده‌کننده عوض شد، نه کدام فایل.
شمارهٔ نسخه همان است که پایین منوی برنامه و در `/api/health` دیده می‌شود.

## 1.1.0 — ۷ شهریور ۱۴۰۵

### زبان تصویری تازه

- **کاشی‌های آیکون (App Tile):** مربعِ گردِ نورخورده با همان ریگ نورپردازی
  سکه‌های سه‌بعدی — روی ردیف‌های منو، عنوان هر صفحه، حالت‌های خالی و سربرگ
  بخش‌های هر دو پنل. هفت رنگ، هفت معنا: پول سبز، اختیار سرمه‌ای، افراد آبی،
  راهنما فیروزه‌ای، هشدار کهربایی، سند خاکستری، خطا گلی.
- **۶۸ صحنهٔ متحرک اختصاصی** برای لحظه‌های همین کسب‌وکار، همه سرِ جای خودشان
  در محصول: چرخهٔ کامل حواله (هر ۱۸ وضعیت سفارش، تصویردار — تضمین‌شده در
  build)، احراز هویت و حکم‌هایش، حساب و ریل بانکی، بازار و طلا، پاداش،
  پشتیبانی، پنل صرافی و پلتفرم، و حالت‌های خالی/خطا. زیر «کاهش حرکت»
  سیستم‌عامل همه ثابت می‌شوند و هیچ‌کدام ناپدید نمی‌شوند.
- بخش‌های «کاشی‌های آیکون» و صحنه‌ها در `/design` مستند شدند؛ قاعده‌ها در
  `docs/brand.md`.

### ناوبری به سبک اپ بومی

- زبانهٔ «پروفایل» به‌جای «منو»؛ کشوی همبرگری فقط برای آنچه جای دیگری ندارد
  (پنل‌ها، صفحات شرکت، متن‌های حقوقی — هرکدام با آیکن خودش).
- سکه و طلا، بازار همتا و پیگیری سفارش به صفحهٔ اول آمدند.
- صفحهٔ پروفایل بازطراحی شد: کارت هویت با تصویرِ حکم (تأیید، در بررسی، نقص
  مدرک، رد)، فهرست «حساب من»، امنیت، دستگاه‌ها و خروج.

### نصب روی گوشی

- **اندروید:** کارت «آسا را روی صفحهٔ اصلی بگذارید» با پیشنهاد نصب خودِ
  مرورگر. یک‌بار «نه» یعنی نه.
- **آیفون:** سافاری پیشنهاد نصب ندارد؛ راهنمای دومرحله‌ای با صحنه‌ای که
  خودِ حرکت را اجرا می‌کند (ضربه روی اشتراک‌گذاری، بالا آمدن شیت).
- `pnpm pwa <url>` صحت مانیفست، آیکن‌ها و سرویس‌ورکر را روی دامنهٔ واقعی
  ممیزی می‌کند.

### رفع اشکال

- **قفل نرخ حواله:** دکمهٔ «دریافت نرخ جدید» پس از پایان ۱۵ دقیقه واقعاً نرخ
  تازه می‌گیرد و شمارنده از نو شروع می‌شود؛ برگهٔ خواب‌رفته هم دیگر نرخ
  مرده را زنده نشان نمی‌دهد.
- **مسدودسازی حساب** در پنل مدیریت حالا واقعاً اثر دارد (RPC اختصاصی + بستن
  ثبت سفارش برای حساب مسدود) و دکمهٔ آن دیگر بی‌عمل نیست.
- **هر ۴۰ بارگذاریِ آزموده زیر «کاهش حرکت» بدون خطای hydration** — پیش از
  این، هر صفحه برای کاربرانِ کاهش‌حرکت یک‌بار کامل دور ریخته و از نو ساخته
  می‌شد (خطای React #418). ریشه: شاخه‌شدن ساختار/`initial` روی ترجیحی که
  سرور نمی‌داند. قاعدهٔ تازه: DOM یکسان، ترجیح فقط روی `transition`.
- ردیف سفارش‌ها در عرض گوشی دیگر تاریخ را کلمه‌به‌کلمه نمی‌شکند.
- برچسب کم‌کنتراست در `/design` که ممیزی WCAG می‌گرفت، اصلاح شد.
- روکشِ «در حال خواندن مدرک» هنگام آپلود، زیر کاهش حرکت هم دیده می‌شود
  (اطلاع است، نه تزئین).

### زیرساخت

- اسنپ‌شات دیپلوی: `deploy/` با ۱۰ اسکریپت، مستند فارسی گام‌به‌گام و
  چک‌لیست راه‌اندازی.
- ۱۰۶ تست واحد، ۴۲ تست e2e (شامل ممیزی WCAG در دو تم و دو زبان)، بودجهٔ
  حجم per-route در CI، و پنج کاتالوگ زبان همگام.

## 1.0.0

نخستین نسخهٔ کامل: بازارگاه حواله (مشتری + پنل صرافی + پنل مدیریت)، نرخ
زنده، احراز هویت چندمرحله‌ای با OCR گذرنامه، ماشین وضعیت ۱۸مرحله‌ای سفارش با
دفترداری دوطرفه، گفت‌وگوی سفارش و پشتیبانی، بازار همتا با ضمانت صرافی، سکه و
طلا، سطح‌بندی و دعوت، PWA آفلاین، و امنیت پایه (RLS همه‌جا، بدون کلید
service-role، CSP سخت‌گیرانه).
